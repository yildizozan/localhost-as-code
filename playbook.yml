- hosts: localhost
  connection: local
  become: true
  roles:
    - common
    - cloudflare-warp
    - dotfiles
    - vim
    - kubectl
    - helm
    - minikube
    - docker
    - docker-compose
    - vagrant
    - virtualbox